你好！我是**跨境电商售后客服分析助手** 👋

请提供以下**至少一项**，我即可为你查询买卖双方历史邮件与历史 AI 处理记录：

- **订单编号** `order_id`（精确匹配）
- **买家邮箱** `buyer_email`（盲匹配：长邮箱取 `+` 前部分，短邮箱取 `@` 前部分，自动匹配同一买家的不同邮箱变形）

> ⚠️ 两项**至少需提供一项**，否则无法定位对话。两者同时提供时合并查询（OR 逻辑）。

**可选** · 查询深度：如不指定，默认 `Email4Final` 取最新 **3** 条、`AIAfterSalesGuide` 取最新 **1** 条。

---

📌 我会基于查询到的历史邮件、历史 AI 处理记录、订单与快递信息、NocoBase 售后知识库，分析当前售后案件并输出一条结构化 JSON，包含：

- 历史沟通总结、当前核心诉求、事实摘要、案件背景及风险提示
- 售后问题类型、处理阶段、AI 建议方案
- **卖家回信正文参考**（给买家的邮件草稿）
- 内部行动指南、人工确认标志、AI 置信度、原因解释

落库后，我会调用 MCP `refresh_after_sales_guide_downloads` 为你生成**邮件历史 CSV** 与**实时快递详情 TXT** 两份下载快照并回填链接，供你核查 AI 判断依据。

---

📝 **关于填表**：生成 JSON 后，我会询问你是否需要填入「AI 驱动售后处理指南」表。若需要，请在 AI 对话框**左下角**点击"加号+三个方块"按钮 → 选择"**选择区块**"→ 鼠标移到 UI 界面中的「AI 驱动售后处理指南」表（**蓝色高亮**即选中）→ 点击"添加"→ 点击红色 AI 人头像 → AI 输出后自动填表。填表完成后，请将**新记录的 id** 发给我，我会调用 MCP 为你刷新下载链接（邮件历史 CSV + 实时快递详情 TXT）。
