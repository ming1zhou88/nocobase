# Third-party data source connectors

Independent AGPL-3.0-only data source connectors for NocoBase.

Initial supported connectors:

- MySQL
- PostgreSQL
- NocoBase HTTP API

All connectors register into the existing NocoBase Data Source Manager. SQL
connectors default to read-only mode and can be explicitly switched to
read/write after the remote account permissions have been reviewed.
